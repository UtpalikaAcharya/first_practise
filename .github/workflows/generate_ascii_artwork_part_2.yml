name: second attempt to generate ascii code
on: push

jobs:
    first_job_part_2:
        runs-on: ubuntu-latest
        steps:
            - name: use checkout
              uses: actions/checkout@v4

            - name: list files
              run: ls -ltra

            - name: create artwork
              run: .github/workflows/ascii_script.sh
              
            